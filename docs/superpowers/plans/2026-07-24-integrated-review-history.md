# PanLayer 今日复盘与历史数据整合实施计划

## Task 1：建立历史概览映射

- 新增 `lib/history/overview.ts`，把 `HistoryRow` 映射为顶部六张卡片所需的稳定结构。
- 对缺失值保持 `null`，不做零值或旧值回填。
- 新增单元测试覆盖完整记录和缺失记录。

## Task 2：打通日期选择

- 为 `HistoryWorkspace` 增加 `onSelectedRowChange` 回调。
- 初始恢复、日历点击和表格点击后都向父组件提交当前真实记录。
- 保留原有排序、滚动位置和会话恢复行为。

## Task 3：整合 Dashboard 信息架构

- 在 `Dashboard` 提升当前历史记录状态。
- 当前交易日继续使用通过校验的实时数据，历史交易日使用映射后的历史快照。
- 将历史日历和表格移动到顶部概览卡片下方，删除页面底部重复历史区块。
- 同步更新标题、顶部日期、状态、来源和更新时间。

## Task 4：验证

- 运行历史映射与 UI 合同测试。
- 运行完整测试、类型检查和生产构建。
- 检查桌面端与移动端的冻结表头、横向滚动和日期联动。

